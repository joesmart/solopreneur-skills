---
step_id: "3.2"
name: "Fill Template"
phase: 3
min_loops: 2
---

# Step 3.2: Fill Template

**Apply the ReAct protocol from SKILL.md to execute this step.**

## Action

100% follow the loaded template's section structure and format. For each section:

1. Replace all `[placeholders]` with real scan data from Phase 2
2. Remove all `<!-- comments -->`
3. Draw ASCII call relationship diagrams using characters: `┌ ┐ └ ┘ ├ ┤ ┬ ┴ ┼ │ ─ → ← ↑ ↓ ▶ ◀ ▲ ▼`
4. Symbol legend: `→` HTTP sync call, `⇢` MQ async notification, `─` database connection

**Hard constraints:**
- **NO** placeholders or template examples in output — all content must be real scan data
- **NO** omitting any template section — every section must exist and be complete
- **NO** reordering sections — section order must match template exactly
- **NO** inventing new sections — only sections defined in template are allowed

## Checklist

Use these items in your **[CHECK]** stage:

- Every template section filled with real data
- No placeholder text remaining (search for `[` characters)
- ASCII diagram accurately reflects Phase 2 call relationships
- Section order matches template exactly
- All comments removed

## Challenges

Use these questions in your **[CHALLENGE]** stage:

- Q1: Observation 异常? → Are there sections that feel thin or incomplete?
- Q2: 解读正确? → Does the ASCII diagram accurately represent all call relationships?
- Q3: 还有遗漏? → Should I go back to Phase 2 scan results to extract more detail?

## Minimum Loops

You must complete at least **2 loops** before you can PASS this step.
